name: Java CI/CD Pipeline to Tomcat

on:
  push:
    branch: [ main ] # Trigger on push to main branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. checkout code
      - name: checkout code
        uses: actions/checkout@v4
    
      # 2. set up jdk
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      # 3. build with maven
      - name: Build with Maven
        run: mvn clean package - DskipTests
     
      # 4. upload war as artificat (optional, for later jobs)
      - name: Upload WAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: myapp
          path: target/*.war

deploy:
  runs-on: ubuntu-latest
  needs: build
  environment: production

  # steps:
  #  # 5. download war artifact
  #  - name: Deploy to Tomcat
  #    run:
  #     WAR_FILE=$(Is *.war)
  #     echo "Deploying @WAE_FILE to Tomcat..."
  #     curl -v --upload-file "$WAR_FILE" \
  #       "http://${{ secrets.TOMCAT_USER }}:${{ secrets.TOMCAT_PASSWORD }}@${{ secrets.TOMCAT_HOST }}/manager/text/deploy?path=/myapp&update=true"
